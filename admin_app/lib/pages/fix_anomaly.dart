import 'package:flutter/material.dart';

class FixAnomaly extends StatelessWidget {
  const FixAnomaly({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
